services:
  postgres:
    image: postgres:15-alpine
    container_name: data-pg-curso-dev
    restart: always
    network_mode: host
    env_file:
      - .env
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
