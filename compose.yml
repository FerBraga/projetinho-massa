version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: data-pg-curso-dev
    restart: always
    network_mode: host
    env_file:
      - .env
    volumes:
      - ./data-pg:/var/lib/postgresql/data
